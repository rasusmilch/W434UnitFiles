//Output for the command VoltageTest
template VoltageTest
begin
   Name = get_teststep_name(RES_Name, LocalText('48#Voltage'));
   switch (RES_Result)
   begin
      case TESTSTEP_Passed:
      begin
         ResultText = LocalText('5#Passed');
      end;
      case TESTSTEP_Failed:
      begin
         if (RES_Value > RES_MaxVoltage)
         begin
            ResultText = LocalText('12#Too high');
         end
         else
         begin
            ResultText = LocalText('13#Too low');
         end;
      end;
      default:
      begin
         ResultText = LocalText('7#Invalid');
      end;
   end;
   ResultText = format_teststep_result(ResultText);
   Name = format_teststep_name(Name);
   Pin1 = format_teststep_pin(get_pinname(RES_Pin1));
   Pin2 = format_teststep_pin(get_pinname(RES_Pin2));
   Value = get_voltage_value(RES_Prefix, RES_Value);

   Line = StrAdd(ResultText, Name);
   Line = StrAdd(Line, Pin1);
   Line = StrAdd(Line, Pin2);
   Line = StrAdd(Line, Value);
   if (RES_Result == TESTSTEP_Passed)
   begin
      UIWriteNormal(Line);
   end
   else
   begin
      UIWriteError(Line);
   end;
end;

//Output for the command VoltageTestCustom
template VoltageTestCustom
begin
   Name = get_teststep_name(RES_Name, LocalText('48#Voltage'));
   switch (RES_Result)
   begin
      case TESTSTEP_Passed:
      begin
         ResultText = LocalText('5#Passed');
      end;
      case TESTSTEP_Failed:
      begin
         if (RES_Value > RES_MaxVoltage)
         begin
            ResultText = LocalText('12#Too high');
         end
         else
         begin
            ResultText = LocalText('13#Too low');
         end;
      end;
      default:
      begin
         ResultText = LocalText('7#Invalid');
      end;
   end;
   ResultText = format_teststep_result(ResultText);
   Name = format_teststep_name(Name);
   Pin1 = format_teststep_pin(get_pinname(RES_Pin1));
   Pin2 = format_teststep_pin(get_pinname(RES_Pin2));
   Value = get_voltage_value(RES_Prefix, RES_Value);

   Line = StrAdd(ResultText, Name);
   Line = StrAdd(Line, Pin1);
   Line = StrAdd(Line, Pin2);
   Line = StrAdd(Line, Value);
   if (RES_Result == TESTSTEP_Passed)
   begin
      UIWriteNormal(Line);
   end
   else
   begin
      UIWriteError(Line);
   end;
end;

//Output for the command VoltageFrequencyTest
template VoltageFrequencyTest
begin
   Name = get_teststep_name(RES_Name, LocalText('165#Voltage+Frequency'));
   switch (RES_Result)
   begin
      case TESTSTEP_Passed:
      begin
         ResultText = LocalText('5#Passed');
      end;
      case TESTSTEP_Failed:
      begin
         ResultTextU = LocalText('270#U') + ': ' + LocalText('122#Fail');
         ResultTextf = LocalText('271#f') + ': ' + LocalText('122#Fail');
         if (RES_VoltagePassed)
         begin
            ResultTextU = LocalText('270#U') + ': ' + LocalText('121#Pass');
         end;
         if (RES_FrequencyPassed)
         begin
            ResultTextf = LocalText('271#f') + ': ' + LocalText('121#Pass');
         end;
         ResultText = ResultTextU + '; ' + ResultTextf;
      end;
      default:
      begin
         ResultText = LocalText('7#Invalid');
      end;
   end;
   ResultText = format_teststep_result(ResultText);
   Name = format_teststep_name(Name);
   Pin1 = format_teststep_pin(get_pinname(RES_Pin1));
   Pin2 = format_teststep_pin(get_pinname(RES_Pin2));
   ValueU = get_voltage_value(RES_Prefix, RES_Value);
   Valuef = get_frequency_value(RES_fPrefix, RES_fValue);
   Value = ValueU + '; ' + Valuef;

   Line = StrAdd(ResultText, Name);
   Line = StrAdd(Line, Pin1);
   Line = StrAdd(Line, Pin2);
   Line = StrAdd(Line, Value);
   if (RES_Result == TESTSTEP_Passed)
   begin
      UIWriteNormal(Line);
   end
   else
   begin
      UIWriteError(Line);
   end;
end;
